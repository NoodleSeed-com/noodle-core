/**
 * Brand-kit visual token derivation: seed colors in, coherent accessible widget CSS tokens out.
 * Pure and dependency-free by charter — no I/O, no CSS assembly, trivially unit-testable — so the
 * compiler, the runtime branding normalization, and host renderers all share one derivation.
 */

export * from './palette.js';
export * from './ui-style.js';
