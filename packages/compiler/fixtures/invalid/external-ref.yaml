# Invalid: inputSchema uses an external `$ref` URI (only local "#/" references are allowed).
manifestVersion: "1"
server:
  name: acme_support
  version: 1.0.0
  title: Acme Support
tools:
  - name: get_order
    description: Look up an order.
    inputSchema:
      type: object
      properties:
        order:
          $ref: "https://example.com/order.json"
    fulfilment:
      use: acme.get_order
