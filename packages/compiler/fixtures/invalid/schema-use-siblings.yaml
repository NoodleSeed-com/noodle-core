# Invalid: $use must be the sole key of its object; sibling keywords are not allowed.
manifestVersion: "1"
server:
  name: acme_support
  version: 1.0.0
  title: Acme Support
schemas:
  address:
    type: object
tools:
  - name: get_order
    description: Look up an order.
    inputSchema:
      type: object
      properties:
        x:
          $use: address
          description: an address
    fulfilment:
      use: acme.get_order
