# Invalid (resolution): connector `acme_orders` v1.2.0 exists, but has no `delete_everything`
# operation. Only surfaces when compiling with a catalog.
manifestVersion: "1"
server:
  name: acme_support
  version: 1.0.0
  title: Acme Support
connectors:
  acme:
    id: acme_orders
    version: 1.2.0
tools:
  - name: nuke
    description: Attempt an operation the connector does not expose.
    inputSchema:
      type: object
    fulfilment:
      use: acme.delete_everything
