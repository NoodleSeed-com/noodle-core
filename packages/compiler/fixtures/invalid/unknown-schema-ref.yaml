# Invalid: $use (nested inside anyOf) references a schema name that the `schemas` map does not
# declare.
manifestVersion: "1"
server:
  name: acme_support
  version: 1.0.0
  title: Acme Support
schemas:
  address:
    type: object
tools:
  - name: get_order
    description: Look up an order.
    inputSchema:
      type: object
      properties:
        x:
          anyOf:
            - type: "null"
            - $use: ghost
    fulfilment:
      use: acme.get_order
